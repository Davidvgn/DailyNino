<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyNino" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DailyNino" parent="Base.Theme.DailyNino" />

    <style name="Theme.DailyNino.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.DailyNino.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.DailyNino.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>